# 3D Print Logger - Environment Variables
# Copy this file to .env and update with your settings

# Application port (default: 8000)
APP_PORT=8000

# MySQL Configuration (if using MySQL instead of SQLite)
# Uncomment and configure these if you enable the MySQL container
# SECURITY WARNING: Always use unique, strong passwords for production deployments!
# - Use different passwords for root and application user
# - Minimum 16 characters with mixed case, numbers, and symbols
# - Never reuse passwords from other systems
# DB_ROOT_PASSWORD=your_secure_root_password
# DB_DATABASE=printlog
# DB_USER=printlog
# DB_PASSWORD=your_secure_password
